<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Shopping Cart</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link rel="stylesheet" href="{{ asset('css/show-cart-style.css') }}">
</head>
<body>
<div class="nav">
  <a href="/catalog">Catalog</a>
  <a href="/user/logout">Log Out</a>
  <a href="/cart">Cart</a>
</div>
<div class="product-card">
  {% for product in products %}
    <div class="preview">
      <h1 class="preview__header">{{ product.name }}</h1>
      <p>{{ product.description }}</p>
      <p>Size: {{ product.size }}</p>
      <p>Price: {{ product.price }} $</p>
      {% if product.imagePath is not null %}
        <img class="preview__image" src="{{ product.imagePath }}" alt="Product Image">
      {% endif %}
      <p>Quantity: {{ cart[product.id] }}</p>
      <a href="/cart/product/remove?id={{ product.id }}" class="btn btn-danger">-</a>
      <a href="/product/add?id={{ product.id }}" class="btn btn-primary">+</a>
      <a href="/cart/product/all/remove?id={{ product.id }}" class="btn btn-danger">Remove</a>
    </div>
  {% endfor %}
</div>
</body>
</html>