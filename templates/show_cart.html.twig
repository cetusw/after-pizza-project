<!DOCTYPE html>
<html lang="en">
<head>
  <title>Catalog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link rel="stylesheet" href="{{ asset('css/show-cart-style.css') }}">
</head>
<body>

<div class="nav">
  <a href="/catalog">Catalog</a>
  <a href="/cart">Cart</a>
  <a href="/user/logout">Log Out</a>
</div>
<div class="d-flex flex-wrap">
  {% for product in products %}
    <div class="card m-2" style="width: 18rem;">
      <img class="card-img-top" src="uploads/{{ product.imagePath }}" alt="Product Image">
      <div class="card-body">
        <a href="/product/show?id={{ product.id }}"><h2 class="card-title">{{ product.name }}</h2></a>
        <p>
          {{ product.description }}<br>
          Size: {{ product.size }}<br>
          {% if cart[product.id] is defined %}
            Price: ${{ product.price * cart[product.id]}}
          {% else %}
            Price: ${{ product.price }}
          {% endif %}
        </p>
        <span>
        <a href="/cart/product/remove?id={{ product.id }}" class="btn btn-danger">-</a>
        <a href="/product/add?id={{ product.id }}" class="btn btn-primary">+</a>
        <a href="/cart/product/all/remove?id={{ product.id }}" class="btn btn-danger">Remove</a>
        {{ cart[product.id] }}
        </span>
      </div>
    </div>
  {% endfor %}
  {% if products|length > 0 %}
    <div class="m-2">
      <a href="/order" class="btn btn-primary w-100">Order</a>
    </div>
  {% endif %}
</div>
</body>
</html>