<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Product Card</title>
    <link rel="stylesheet" href="/css/product-card-style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
<div class="card">
    <h1 class="card__title">Product Information</h1>
    <div class="card-body">
        <div class="card-group">{{ product.name }}</div>
        <div class="card-group">{{ product.description }}</div>
        <div class="card-group">{{ product.size }}</div>
        <div class="card-group">{{ product.price }}</div>
        <div class="card-group">{{ product.imagePath }}</div>
        <a href="/catalog" class="btn btn-primary">Go to Store</a>
    </div>
</div>
</body>
</html>